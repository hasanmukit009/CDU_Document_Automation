@model IEnumerable<DMS.Web.Models.UnitInformationModel>
@{
    ViewData["Title"] = "UnitInfoListAdmin";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="container-fluid">
    <div class="row mb-2">
        <div class="col-sm-6">
            <h1>@*Unit Information List*@</h1>
        </div>
        <div class="col-sm-6">
            <ol class="breadcrumb float-sm-right">
                <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Dashboard">Home</a></li>
                <li class="breadcrumb-item active">Unit Information</li>
            </ol>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <table id="tblUnitListAdmin" class="table table-bordered table-striped overflow-auto">
                        <thead>
                            <tr>
                                <th>Activity Log</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Remarks)
                                    </td>
                                    @if (User.Identity.Name == item.Admin)
                                    {
                                        <td>
                                            <a asp-action="ViewUnitInformationByAdmin" asp-route-id="@item.Id" class="btn btn-primary btn-icon-split">
                                                <span class="text">View</span>
                                            </a>
                                            @if (String.IsNullOrEmpty(item.Reviewer1) || String.IsNullOrEmpty(item.Reviewer2))
                                            {
                                                <a asp-action="AssignToReviewer" asp-route-id="@item.Id" class="btn btn-primary btn-icon-split disabled">
                                                    <span class="text">Assign To Reviewer</span>
                                                </a>
                                            }
                                            else
                                            {
                                                <a asp-action="AssignToReviewer" asp-route-id="@item.Id" class="btn btn-primary btn-icon-split">
                                                    <span class="text">Assign To Reviewer</span>
                                                </a>
                                            }
                                        </td>
                                    }
                                    else
                                    {
                                        <td>
                                            <a asp-action="ViewUnitInformationByAdmin" asp-route-id="@item.Id" class="btn btn-primary btn-icon-split disabled">
                                                <span class="text">View</span>
                                            </a>
                                            <a asp-action="AssignToReviewer" asp-route-id="@item.Id" class="btn btn-primary btn-icon-split disabled">
                                                <span class="text">Assign To Reviewer</span>
                                            </a>
                                        </td>
                                    }
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
        <!-- /.col -->
    </div>
    <!-- /.row -->
</div>